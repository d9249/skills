interface:
  display_name: "PP-OCR Commit Sync"
  short_description: "pp-ocr 커밋을 ocr 레포로 안전하게 미러링"
  default_prompt: "Use $pp-ocr-ocr-sync to mirror these pp-ocr commits into the paired ocr repo and verify the synced paths."
